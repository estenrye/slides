apiVersion: v1
kind: Service
metadata:
  name: {{ include "mssql_server.fullname" . }}
  labels:
    {{- include "mssql_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mssql_server.selectorLabels" . | nindent 4 }}
