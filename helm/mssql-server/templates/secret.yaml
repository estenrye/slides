apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mssql-server.fullname" . }}-mssql-secret
  labels:
    {{- include "mssql-server.labels" . | nindent 4 }}
type: Opaque
data:
 sa_password : {{ .Values.sa_password | b64enc | quote}}
