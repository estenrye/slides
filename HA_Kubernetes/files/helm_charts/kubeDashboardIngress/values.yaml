# Default values for kubeDashboardIngress.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
ingressClassName: nginx
certManagerIssuer: prod-acme-cloudflare-issuer
zone: example.com
kubeDashboardReleaseName: kube-dashboard
