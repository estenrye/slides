apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeDashboardIngress.fullname" . }}-test-connection"
  labels:
    {{- include "kubeDashboardIngress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['kubernetes.{{ .Values.zone }}:443']
  restartPolicy: Never
