---
- hosts: all
  tasks:
    - name: update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      become: true
    - name: apt upgrade
      ansible.builtin.apt:
        upgrade: yes
      become: yes

- hosts: external_load_balancer
  roles:
    - haproxy
