- hosts: localhost
  connection: local
  roles:
    - add_apt_repos
    - ansible_galaxy_packages
    - python_packages
    - terminal
    - discord
    - docker
    - firewall
    - git
    - microsoft_teams
    - slack
    - visual_studio_code
    - yubikey
